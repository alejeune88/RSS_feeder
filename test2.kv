#:kivy 1.0

<Article_layout>:
    height: 40
    id: al1
    size_hint: (1,None)

    Label:
        id: title
        text: self.ids.title.article_list
        size_hint: (0.5,0.4)
        pos_hint: {'x':0.0, 'top':1}

    